[tasks.build]
description = "Compile TypeScript to dist/ (tsc)"
run = "pnpm build"

[tasks.typecheck]
description = "Type-check without emitting"
run = "pnpm typecheck"

[tasks.test]
description = "Run node:test suites"
run = "pnpm test"
